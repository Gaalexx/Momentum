<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Добавьте разрешение на интернет для загрузки изображений -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Momentum"
        android:usesCleartextTraffic="true"> <!-- для http изображений -->
        android:requestLegacyExternalStorage="true"> <!-- Для Android 10+ -->

        <!-- Frame75Activity СДЕЛАНА ГЛАВНОЙ -->
        <activity
            android:name=".Frame75Activity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Momentum">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity оставлена как дополнительная -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Momentum">
            <!-- Убрать intent-filter, если не нужно запускать -->
        </activity>

    </application>

</manifest>